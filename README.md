# 🏕️ Camp Booking Platform (แพลตฟอร์มจองค่ายในประเทศไทย)

ระบบบริหารจัดการและจองค่ายกิจกรรม (Camp Booking System) พัฒนาด้วยภาษา **Python** โดยใช้ **CustomTkinter** ในการออกแบบหน้าจอ User Interface (GUI) ที่มีความสวยงามและทันสมัย รองรับการใช้งานทั้งในฝั่งของผู้เข้าร่วมกิจกรรม (User) และผู้จัดกิจกรรม (Organizer)

---

## ✨ ฟีเจอร์หลัก (Key Features)

### 👤 สำหรับผู้ใช้งานทั่วไป (User)
* **ระบบสมาชิก:** สมัครสมาชิก (Register) และเข้าสู่ระบบ (Login) เพื่อเริ่มใช้งาน
* **ค้นหาและจองค่าย:** เลือกดูรายการค่ายต่างๆ ตรวจสอบจำนวนที่นั่งว่าง และกดจองได้ทันที
* **ประวัติการจอง:** ดูรายการค่ายที่ตนเองได้ทำการจองไว้
* **QR Code:** สร้าง QR Code ที่แสดงรายละเอียดของค่ายเพื่อนำไปแชร์ต่อได้

### 🏢 สำหรับผู้จัดค่าย (Organizer)
* **จัดการค่าย:** สร้างค่ายใหม่, แก้ไขรายละเอียด, หรือลบค่ายกิจกรรมออกจากระบบ
* **ตรวจสอบผู้เข้าร่วม:** ดูรายชื่อผู้ที่จองค่ายของคุณ พร้อมข้อมูลติดต่อ (เบอร์โทร, อีเมล)
* **แดชบอร์ด:** ดูสถานะยอดการจองและที่นั่งที่เหลืออยู่

### 🔔 ระบบอื่นๆ
* **การแจ้งเตือน (Notifications):** แจ้งเตือนทันทีที่มีการจองค่ายสำเร็จ
* **ระบบฐานข้อมูล (File-based):** บันทึกข้อมูลทั้งหมดลงไฟล์ JSON (Users, Camps, Bookings) โดยไม่ต้องติดตั้ง Database Server เพิ่มเติม

---

## 🛠️ ความต้องการระบบ (Requirements)

* **OS:** Windows, macOS, หรือ Linux
* **Python:** เวอร์ชัน 3.x ขึ้นไป (แนะนำ 3.10+)

---

## 📦 ขั้นตอนการติดตั้งและลง Library (Installation)

โปรเจกต์นี้มีการใช้งาน Library ภายนอก จำเป็นต้องติดตั้งก่อนเริ่มรันโปรแกรม โดยทำตามขั้นตอนดังนี้:

### 1. เตรียมโปรเจกต์
ดาวน์โหลดหรือ Clone โปรเจกต์นี้มาไว้ที่เครื่องของคุณ และเปิด **Terminal** (หรือ Command Prompt / PowerShell) ไปยังโฟลเดอร์ที่เก็บไฟล์

### 2. ติดตั้ง Library ที่จำเป็น
โปรแกรมนี้ใช้ 3 Library หลัก ได้แก่ `customtkinter`, `Pillow`, และ `qrcode` (ส่วน `json`, `os`, `datetime` นั้นติดมากับ Python อยู่แล้ว ไม่ต้องลงเพิ่ม)

พิมพ์คำสั่งด้านล่างลงใน Terminal เพื่อติดตั้ง:

**วิธีที่ 1: ติดตั้งทีละตัว (แนะนำสำหรับผู้เริ่มต้น)**
```bash
# 1. ติดตั้ง CustomTkinter (สำหรับหน้าจอ GUI สวยๆ)
pip install customtkinter

# 2. ติดตั้ง Pillow (สำหรับจัดการรูปภาพ)
pip install Pillow

# 3. ติดตั้ง QRCode (สำหรับสร้าง QR Code)
pip install qrcode
```
### วิธีการรันโปรแกรม (Usage)
เมื่อติดตั้งทุกอย่างเสร็จสิ้น สามารถเปิดใช้งานโปรแกรมได้ด้วยคำสั่ง:
คำแนะนำการใช้งานเบื้องต้น
หน้า Login: เมื่อเปิดโปรแกรมจะพบหน้าเข้าสู่ระบบ

หากยังไม่มีบัญชี ให้กดปุ่ม Register เพื่อสมัครใหม่

หรือใช้บัญชีทดสอบที่มีอยู่ในระบบ (ดูในไฟล์ users.json)

เมนูหลัก:

หน้าผู้ใช้ (จองค่าย): สำหรับดูค่ายที่มีและกดจอง

หน้าผู้จัดค่าย: สำหรับสร้างค่ายของตัวเอง

